// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var errorMessage, form, removeMessage, submitData, successMessage, url;
  form = $('#ftp-settings-form');
  url = form.attr('action');
  successMessage = templates.ftpSettingsSaveOK;
  errorMessage = templates.ftpSettingsSaveError;
  removeMessage = function() {
    return (form.find('.o-form-message')).slideUp(function() {
      return ($(this)).remove();
    });
  };
  submitData = function(data) {
    var res;
    res = $.post(url, data);
    res.done(function(data) {
      form.append(successMessage);
      (form.parents('.o-collapsible-section')).trigger('remax');
      return setTimeout(removeMessage, 3000);
    });
    return res.fail(function() {
      return form.append(errorMessage);
    });
  };
  return form.on('submit', function(e) {
    var data;
    e.preventDefault();
    data = form.serialize();
    return submitData(data);
  });
})(this, this.jQuery, this.templates);
